<% provide(:title, "Sujets") %>

<%= render 'layouts/last_statements' %>

<%= link_to "Nouveau", new_subject_path, class: "btn btn-primary" %>

<div class="subjects-index" >
    <%= render @subjects %>
</div>
<%= will_paginate %>