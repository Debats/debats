<% provide(:title, "Sujets") %>

<h1>Sujets <%= link_to "(+ Créer un sujet)", new_subject_path, if  allowed_to(:add_subject) %></h1>


<div class="subjects-index col-md-8" style="margin-bottom:20px">

<%= render @subjects %>
<%= will_paginate %>
</div>

<div class="col-md-4">
<%= render 'layouts/last_statements' %>
</div>
